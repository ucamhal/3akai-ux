define([
    'underscore',
    'text!/avocet/templates/form.html'
], function(_, template) {
    'use strict';

    var Form = function() {

    };

    _.extend(Form.prototype, {

    });

    return Form;
});
